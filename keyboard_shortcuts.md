esc - minimize
del - delete
alt + s = settings
alt + d = draw tool
alt + m = maximize/restore (depending on current state)
alt + r = reset widget to default size
alt + esc = minimize all widgets to doc
alt + del = delete all widgets from board
alt + --> = select next board to the right in the selection bar
alt + <-- = select next board tot he left in the selection bar

Swipe gestures to audit/update/implement
4 finger swipe down = minimize all widgets to dock
4 finger swipe right to left = select next board to the right
4 finger swipe left to right = select next board to the left
pinch in/out = progressively zoom in/out at pinch location
