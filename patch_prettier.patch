--- components/widgets/Schedule/Settings.tsx
+++ components/widgets/Schedule/Settings.tsx
@@ -395,9 +395,9 @@
   // ── Render ─────────────────────────────────────────────────────────────────

   const validItems =
-    selectedSchedule?.items.filter(
-      (i): i is ScheduleItem & { id: string } => !!i.id
-    ) ?? [];
+    selectedSchedule?.items.filter(
+      (i): i is ScheduleItem & { id: string } => !!i.id
+    ) ?? [];

   return (
     <div className="space-y-4">
