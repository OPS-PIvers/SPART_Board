--- components/widgets/Scoreboard/Widget.test.tsx
+++ components/widgets/Scoreboard/Widget.test.tsx
@@ -254,8 +254,8 @@
       type: 'random' as WidgetType,
       config: {
         lastResult: [
-          { names: ['Alice', 'Bob'] },
-          { names: ['Charlie', 'Dave'] },
+          { id: 'group-1', names: ['Alice', 'Bob'] },
+          { id: 'group-2', names: ['Charlie', 'Dave'] },
         ],
       } as RandomConfig,
       x: 0,
@@ -269,6 +269,9 @@
       ...mockDashboardContext,
       activeDashboard: {
         widgets: [randomWidget],
+        sharedGroups: [
+          { id: 'group-1', name: 'Custom Group Name 1' },
+        ]
       },
     });

@@ -283,7 +286,7 @@
       expect.objectContaining({
         config: expect.objectContaining({
           teams: expect.arrayContaining([
-            expect.objectContaining({ name: 'Group 1' }),
+            expect.objectContaining({ name: 'Custom Group Name 1', linkedGroupId: 'group-1' }),
             expect.objectContaining({ name: 'Group 2' }),
           ]) as unknown,
         }) as unknown,
