description = "Updates your workspace with the latest changes from the main project."
prompt = """

You are a technical assistant. Execute the following steps exactly:

1. Run `git status --short`. 
    1A) If the output is not empty, run `git stash` and notify the user: "Your current unsaved changes have been temporarily set aside to allow for the update."

2. Run `git fetch origin main`.

3. Run `git rebase origin/main`.

4. If the rebase is successful:

    4A) If you ran `git stash` earlier, run `git stash pop`.

    4B) Run `git push --force-with-lease origin $(git branch --show-current)`.
        - Explain: "I'm updating your online backup to match these new changes."
        - Note: If this fails, it's okay, just proceed to the report.

    4C) Report: "Update complete. Your workspace is now up-to-date with the latest version of the project."

5. If the rebase fails (exit code non-zero):

    5A) Run `git rebase --abort`.

    5B) If you ran `git stash` earlier, run `git stash pop`.

    5C) Identify conflicting files via `git status`.

    5D) Report: "Update paused. There are conflicting changes between your work and the latest updates from Main. To keep your work safe, I have stopped the update.\n\nPlease type this to me now: **'Help me fix the sync conflicts'**\n\nI will then help you manually merge the updates so nothing gets lost." 
"""
