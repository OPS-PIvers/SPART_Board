--- components/widgets/Scoreboard/Widget.test.tsx
+++ components/widgets/Scoreboard/Widget.test.tsx
@@ -470,7 +470,8 @@
     const input = screen.getByPlaceholderText('Team Name');
     fireEvent.change(input, { target: { value: 'New Team Name' } });

-    vi.advanceTimersByTime(500); // Advance debounce time
+    import { act } from '@testing-library/react';
+    act(() => { vi.advanceTimersByTime(500); }); // Advance debounce time

     expect(mockUpdateWidget).toHaveBeenCalledWith(
       'scoreboard-id',
@@ -506,6 +507,9 @@
       },
     });

+    // Clear to ensure we don't pick up previous calls
+    mockAddToast.mockClear();
+
     render(<ScoreboardSettings widget={widget} />);
     const importButton = screen.getByTitle('Add a Randomizer widget first');
     fireEvent.click(importButton);
