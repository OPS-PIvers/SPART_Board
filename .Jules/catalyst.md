## 2025-10-26 - Unified Data Export Utility **Discovery:** Several widgets (`PollWidget`, `ChecklistWidget`, `ScoreboardWidget`) manage tabular data but lacked a unified way to export it. `PollWidget` had a manual implementation. **Opportunity:** Created `utils/export.ts` to centralize CSV export logic, enabling rapid addition of "Export" features to other widgets with minimal code duplication.
