## 2025-02-14 - TrafficLightWidget Coverage
**Gap:** TrafficLightWidget had 0% coverage and was untested.
**Fix:** Created `TrafficLightWidget.test.tsx` using `user-event` and `getByRole` to verify interactions and state updates, bringing coverage to 90%.
